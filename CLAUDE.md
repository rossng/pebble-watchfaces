# CLAUDE.md

Agent guidance for this repo lives in **[AGENTS.md](AGENTS.md)** — read it before
writing or testing a watchface. Key reflexes: build through `pnpm` (not bare
`pebble`), `pnpm build` is the real test (not just `pnpm typecheck`), and
`pnpm reset` clears a boot-looping emulator.

@AGENTS.md
